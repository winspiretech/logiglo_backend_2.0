name: CI/CD Pipeline for logiglo_backend_2.0

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Check out the code
      - name: Checkout code
<<<<<<< HEAD
        uses: actions/checkout@v4

      # Log in to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
=======
        uses: actions/checkout@v3

      # Log in to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
>>>>>>> b71b593c19b495cbe0177161de4938a098073f89
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # Build and push Docker image
      - name: Build and push Docker image
<<<<<<< HEAD
        uses: docker/build-push-action@v5
=======
        uses: docker/build-push-action@v4
>>>>>>> b71b593c19b495cbe0177161de4938a098073f89
        with:
          context: .
          file: ./.github/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/logiglo_backend_2.0:latest

<<<<<<< HEAD
      # SSH into MilesWeb server and deploy
=======
      # Deploy to MilesWeb server
>>>>>>> b71b593c19b495cbe0177161de4938a098073f89
      - name: Deploy to server
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.REMOTE_HOST }}
          username: ${{ secrets.REMOTE_USER }}
          key: ${{ secrets.REMOTE_KEY }}
          script: |
<<<<<<< HEAD
            docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin <<< "${{ secrets.DOCKER_PASSWORD }}"
            docker pull ${{ secrets.DOCKER_USERNAME }}/logiglo_backend_2.0:latest
            cd /logiglo_backend_2.0/container
            docker-compose down
            docker-compose up -d
=======
            # Log in to Docker Hub
            echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
            # Stop and remove existing container (if any)
            docker stop logiglo_backend_2.0 || true
            docker rm logiglo_backend_2.0 || true
            # Pull the latest image
            docker pull ${{ secrets.DOCKER_USERNAME }}/logiglo_backend_2.0:latest
            # Run the new container
            docker run -d --name logiglo_backend_2.0 -p 7000:7000 ${{ secrets.DOCKER_USERNAME }}/logiglo_backend_2.0:latest
            # Clean up unused images
>>>>>>> b71b593c19b495cbe0177161de4938a098073f89
            docker image prune -f
